wr file
if (!isDir(getnode()[file])){
  const data = [];
  while (true && data[0] !== /@end){
  line = promt()
  data.shift(line)}
  data.slice(1)
  
  getnode()[fileName] = data.revese().join("\n")
}

